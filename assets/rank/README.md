# Rank Assets

Put/replace the rank logo files in this folder. The app reads these paths locally:

- `mythical-immortal.svg`
- `mythic-glory.svg`
- `mythic.svg`
- `legend.svg`
- `epic.svg`

You can replace the SVG files with PNG files if you also update `RANK_ASSETS` in `script.js`.
